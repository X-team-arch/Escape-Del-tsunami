-- 🌊 Escape Del Tsunami 2026
local a = loadstring(game:HttpGet('https://sirius.menu/rayfield', true))()

local b, c, d, e, f, g, h = false, false, false, false, false, false, "Mythical"

local i = a:CreateWindow({Name = "Escape Del Tsunami", LoadingTitle = "🌊", KeySystem = false})

local j = i:CreateTab("k", 4483362458)

-- Input de Key con placeholder de rayas
local l = j:CreateInput({
    Name = "Key",
    PlaceholderText = "________________",
    Callback = function(m)
        if string.lower(m) == "5555" then 
            b = true 
            a:Notify({Title = "✅", Content = "Key correcta 🌊", Duration = 4})
            n()
        else 
            a:Notify({Title = "❌", Content = "Key incorrecta", Duration = 3})
        end 
    end
})

-- Botón Get Key
local o = j:CreateButton({
    Name = "Get Key",
    Callback = function()
        local p = "https://robiox.com.af/communities/4362678772/"
        setclipboard(p)
        a:Notify({Title = "📋", Content = "Enlace de Key copiado al portapapeles", Duration = 6})
    end
})

function n()
    local q = i:CreateTab("🌊", 4483362458)
    
    q:CreateToggle({Name = "..", Callback = function(r)
        c = r 
        if r then 
            spawn(function()
                while c do 
                    local s = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid")
                    if s then 
                        s.Health = math.huge 
                        s.MaxHealth = math.huge 
                    end 
                    task.wait(0.5)
                end 
            end)
        end 
    end})
    
    q:CreateToggle({Name = "..", Callback = function(r)
        d = r 
        if r then 
            spawn(function()
                while d do 
                    for _, t in workspace:GetDescendants() do 
                        local u = t.Name:lower()
                        if u:find("wave") or u:find("tsunami") or u:find("water") then 
                            if t:IsA("BasePart") then 
                                t.Transparency = 1 
                                t.CanCollide = false 
                                t.Anchored = true 
                            end 
                        end 
                    end 
                    task.wait(1)
                end 
            end)
        end 
    end})
    
    q:CreateToggle({Name = "..", Callback = function(r) e = r end})
    q:CreateToggle({Name = "..", Callback = function(r) f = r end})
    
    local v = q:CreateDropdown({Name = "..", Options = {"Mythical", "Divine", "Infinite"}, Callback = function(r) h = r end})
    
    q:CreateToggle({Name = "..", Callback = function(r)
        g = r 
        if r then 
            spawn(function()
                while g do 
                    local w = game:GetService("ReplicatedStorage")
                    if w:FindFirstChild("SpawnBrainrot") then 
                        w.SpawnBrainrot:FireServer(h)
                    end 
                    task.wait(0.5)
                end 
            end)
        end 
    end})
    
    local x = i:CreateTab("⚡", 4483362458)
    x:CreateToggle({Name = "..", Callback = function(r)
        if r then 
            for _, y in workspace:GetDescendants() do 
                if y.Name:lower():find("vip") or y.Name:lower():find("barrier") then 
                    y:Destroy()
                end 
            end 
        end 
    end})
    
    x:CreateButton({Name = "..", Callback = function()
        local z = game.Players.LocalPlayer 
        if z.leaderstats then 
            for _, aa in z.leaderstats:GetChildren() do 
                if aa.Name:lower():find("cash") or aa.Name:lower():find("token") then 
                    aa.Value = math.huge 
                end 
            end 
        end 
    end})
    
    a:Notify({Title = "✅", Content = "Menú cargado correctamente", Duration = 5})
end

print("🌊 Escape Del Tsunami Script cargado")
